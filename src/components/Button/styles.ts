import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  button: {
    alignItems: "center",
    backgroundColor: "#7DCE13",

    padding: 15,
    marginTop: 20,

    borderRadius: 7,
  },
  textButton: {
    color: "#ffffff",
    fontSize: 17,
    fontWeight: "bold",
  },
});
